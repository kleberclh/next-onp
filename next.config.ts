import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["i.imgur.com", "cdn.suaimagem.com","imgur.com"], // substitua pelo domínio real da imagem
  },
};

export default nextConfig;
